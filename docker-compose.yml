services:
  app:
    build: .
    container_name: financeiro-app
    restart: always
    environment:
      - DOCKER=true
      - SQLITE_FILE=finance.db
    ports:
      - "3001:3000"
    volumes:
      - ./data:/app/data
